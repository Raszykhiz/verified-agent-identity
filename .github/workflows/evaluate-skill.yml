Testing Code in case the workflow is showing failed.. edit the main.yml and use this code 👇👇👇


name: Verify Human Identity
on:
  workflow_dispatch:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: Verify Identity
        run: echo "Identity Verified"
